import { PadletFormErrorMessages } from './padlet-form-error-messages';

describe('PadletFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new PadletFormErrorMessages()).toBeTruthy();
  });
});
